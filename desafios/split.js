const palavra = ("Criar o que não existe ainda deve ser a pretensão de todo sujeito que está vivo; A tarefa mais importante de uma pessoa que vem ao mundo é criar algo");
const separar = palavra.split(';');

console.log(separar);